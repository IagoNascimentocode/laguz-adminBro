const React = require('react');
const {WrapperBox} = require('admin-bro')

const Dashboard = (props) => {
    return (
      <WrapperBox>My custom dashboard</WrapperBox>
    )
  }
  
  export default Dashboard